---
const pageTitle: string = "Sobre Mim";

const identify = {
  firstName: "João",
  country: "Brasil",
  occupation: "Programador",
  hobbies: ["Programar", "Jogar jogos", "Corrida", "Ler livros"],
};

const skills: Array<string> = [
  "HTML",
  "CSS",
  "JavaScript",
  "TypeScript",
  "Angular",
  "Astro",
  "Escrever Documentação",
];

const happy = true;
const finished = false;
const goal = 3;
---

<html lang="pt-br">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
  </head>
  <body>
    <nav class="nav">
      <ul>
        <li><a href="/">Início</a></li>
        <li><a href="/about/">Sobre</a></li>
        <li><a href="/blog/">Blog</a></li>
      </ul>
    </nav>

    <h1>{pageTitle}</h1>
    <h2>... e meu novo site Astro!</h2>

    <p>
      Estou resolvendo o tutorial introdutório do Astro. Esta é a segunda página
      no meu website, e é a primeira que eu mesmo construí!
    </p>

    <p>
      Este site será atualizado enquanto eu completo mais desse tutorial, então
      continue verificando e veja como minha jornada está indo!
    </p>

    <p>Aqui estão alguns fatos sobre mim:</p>
    <ul>
      <li>Meu nome é {identify.firstName}</li>
      <li>
        Eu sou do {identify.country} e trabalho como {identify.occupation}.
      </li>
      {
        identify.hobbies.length > 0 && (
          <li>Eu gosto de {identify.hobbies.join(", ")}.</li>
        )
      }
    </ul>

    <p>Minhas habilidades são:</p>
    <ul>
      {skills.map((skill) => <li>{skill}</li>)}
    </ul>

    {happy && <p>Estou feliz em estar aprendendo Astro!</p>}

    {finished && <p>Eu terminei esse tutorial!</p>}

    {
      goal === 3 ? (
        <p>Minha meta é terminar em 3 dias.</p>
      ) : (
        <p>Minha meta não é em 3 dias.</p>
      )
    }
  </body>
</html>

<style>
  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
      Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
  }

  .nav {
    padding: 12px 0;
    width: 100%;
    background: #f0f0f0;
  }

  .nav ul {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    list-style: none;
    padding: 0;
  }

  .nav a {
    text-decoration: none;
    color: #333;
  }
</style>
