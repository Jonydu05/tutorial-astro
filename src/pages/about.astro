---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/footer.astro';
import Head from '../components/head.astro';

const pageTitle = 'Sobre Mim';

const identify = {
  firstName: 'João',
  country: 'Brasil',
  occupation: 'Programador',
  hobbies: ['Programar', 'Jogar jogos', 'Corrida', 'Ler livros'],
};

const skills: Array<string> = ['HTML', 'CSS', 'JavaScript', 'TypeScript', 'Angular', 'Astro', 'Escrever Documentação'];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = 'rgba(57, 172, 255)';
const fontWeight = '500';
const fontFamily = 'Lora, serif';
---

<html lang="pt-br">
  <Head pageTitle={pageTitle} />
  <body>
    <Header />
    <main>
      <h1>{pageTitle}</h1>
      <h2>... e meu novo site Astro!</h2>

      <p>
        Estou resolvendo o tutorial introdutório do Astro. Esta é a segunda página no meu website, e é a primeira que eu
        mesmo construí!
      </p>

      <p>
        Este site será atualizado enquanto eu completo mais desse tutorial, então continue verificando e veja como minha
        jornada está indo!
      </p>

      <p>Aqui estão alguns fatos sobre mim:</p>
      <ul>
        <li>Meu nome é {identify.firstName}</li>
        <li>
          Eu sou do {identify.country} e trabalho como {identify.occupation}.
        </li>
        {identify.hobbies.length > 0 && <li>Eu gosto de {identify.hobbies.join(', ')}.</li>}
      </ul>

      <p>Minhas habilidades são:</p>
      <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
      </ul>

      {happy && <p>Estou feliz em estar aprendendo Astro!</p>}

      {finished && <p>Eu terminei esse tutorial!</p>}

      {goal === 3 ? <p>Minha meta é terminar em 3 dias.</p> : <p>Minha meta não é em 3 dias.</p>}
    </main>
    <Footer />
    <style define:vars={{ skillColor, fontWeight, fontFamily }}>
      h1 {
        color: purple;
        font-size: 2rem;
      }

      .skill {
        color: var(--skillColor);
        font-weight: var(--fontWeight);
        font-family: var(--fontFamily);
      }
    </style>
  </body>
</html>
